{# {% extends 'base.html.twig' %}

{% block title %}Event Detail{% endblock %}

{% block body %}
<section class="event-detail spad">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="section-title">
                    <h2>{{ event.title }}</h2>
                </div>
                <div class="st-content">
                    <div class="row">
                        <div class="col-lg-12">
                            <div class="sc-pic">
                                <img src="{{ asset('custom/img/Placeholder.png') }}" alt="">
                            </div>
                        </div>
                        <div class="col-lg-12">
                            <div class="sc-text">
                                <h4>{{ event.title }}</h4>
                                <p>{{ event.description }}</p>
                            </div>
                            <ul class="sc-widget">
                                <li><i class="fa fa-clock-o"></i> {{ event.dateTime|date('M d, Y H:i') }}</li>
                                <li><i class="fa fa-user"></i> Max participants: {{ event.maxParticipants }}</li>
                                <li><i class="fa fa-check"></i> Public: {{ event.isPublic ? 'Yes' : 'No' }}</li>
                            </ul>
                            <a href="{{ path('app_event_register', {'id': event.id}) }}" class="btn btn-primary">Register</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %} #}


{% extends 'base.html.twig' %}

{% block title %}{{ event.title }}{% endblock %}

{% block body %}
<section class="event-detail spad">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="section-title">
                    <h2>{{ event.title }}</h2>
                    <p><i class="fas fa-champagne-glasses"></i> Get ready to mingle and jingle – all the deets you need!</p>
                    <div class="breadcrumb-text">
                        <div class="bt-option">
                            <a href="{{ path('app_events') }}">Events</a>
                            <span>{{ event.title }}</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="schedule-tab">
            <div class="st-content p-4">
                <div class="row align-items-center">
                    <div class="col-sm-12">
                        <div class="speaker-item">
                            <div class="row align-items-center">
                                <div class="col-lg-6">
                                    <div class="si-pic">
                                        <img src="{{ asset('custom/img/Placeholder.png') }}" alt="">
                                    </div>
                                </div>
                                <div class="col-lg-6">
                                    <div class="si-text">
                                        <div class="si-title">
                                            <h4>{{ event.title }}</h4>
                                            <span><i class="fas fa-calendar"></i> Event</span>
                                        </div>
                                        <p><i class="fas fa-pen-to-square"></i> {{ event.description }}</p>
                                    </div>
                                    
                                    <ul class="sc-widget pb-5 pt-0">
                                        <li><i class="fa fa-clock"></i> {{ event.dateTime|date('M d, Y H:i') }}</li>
                                        <li><i class="fa fa-user"></i> Max participants: <b>{{ event.maxParticipants }}</b></li>
                                        <li><i class="fa fa-{{ event.isPublic ? 'lock-open' : 'lock' }}"></i> {{ event.isPublic ? 'Public' : 'Private' }}</li>
                                    </ul>

                                    <div class="ha-text pt-0">
                                        <a href="#" class="ha-btn"><i class="fas fa-file-signature"></i> Subcribe</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}
